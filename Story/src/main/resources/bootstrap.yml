spring:
  application:
    name: StoryMS
  cloud:
    consul:
      config:
        enabled: true
        prefixes: config
        defaultContext: application
        profileSeparator: "::"
        format: YAML
  
server:
  port: 1900
  
