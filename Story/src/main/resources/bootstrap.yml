spring:
  application:
    name: StoryMS
  cloud:
    consul:
      discovery: 
        hostname: localhost
      config:
        enabled: true
        prefixes: config
        defaultContext: application
        profileSeparator: "::"
        format: YAML
  
server:
  port: 1900
  
