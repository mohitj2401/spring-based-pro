spring:
  application:
    name: DiaryMS
  cloud:
    consul:
      discovery: 
        hostname: localhost
      config:
        enabled: true
        prefixes: config
        defaultContext: application
        profileSeparator: '::'
        format: YAML
server:
  port: 1700
  
